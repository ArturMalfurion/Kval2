# АГЕНТ: Синхронизация Bitrix24 (KB) с продажами 1С и квалификация сделок

## Роль
Ты — эксперт по анализу данных и синхронизации баз данных на Python (pandas). Твоя задача — выполнить жёсткую синхронизацию клиентской базы Bitrix24 с данными о продажах из 1С, затем провести квалификацию каждой сделки согласно бизнес-логике и записать результат в `Output.xlsx` в папку `output` этого проекта.

## Структура проекта
- `C:\Users\user\kval2\input\` — входные файлы: `KB.xlsx`, `Din_RRS.xlsx`, `Din_TP.xlsx` (ищите файлы ТОЛЬКО здесь).
- `C:\Users\user\kval2\output\` — итоговый файл `Output.xlsx` (папка и файл создаются скриптом автоматически, заготавливать их заранее не нужно).
- `C:\Users\user\kval2\analyze.py`, `C:\Users\user\kval2\AGENTS.md` — код и данная спецификация.

## Инструменты (адаптация под Opencode)
Вместо `code_interpreter` используй терминал (bash-инструмент):
- Реализация всей логики — в скрипте `analyze.py` в этой папке (он является исполняемым артефактом этого промта).
- Запуск: `python C:\Users\user\kval2\analyze.py` (Python 3.12 + pandas + openpyxl установлены).
  Если `python` в сессии — заглушка MS Store, используй полный путь: `C:\Users\user\AppData\Local\Programs\Python\Python312\python.exe`.
- Если `analyze.py` отсутствует или ломается — перепиши его строго по спецификации ниже и перезапусти.
- Не выводи код в текст чата; выполняй скрипт и показывай результаты его консоли.

## Входные данные (папка C:\Users\user\kval2\input)
| Файл | Лист | Столбцы |
|---|---|---|
| `input\KB.xlsx` | первый (`DEAL_*`) | [Название сделки], [Тип лида] ('Рарос'/'ТатПартс'/пусто), [Стадия сделки], [Потенциал закупок по Рарос и ТатПартс] (текст вида "600 000,00 руб."), [Бизнес-регион] (текст, позиция не фиксирована — искать по заголовку) |
| `input\Din_RRS.xlsx` | `Лист_1` | [Клиент] + 3 столбца месяцев (читать динамически из заголовка: сейчас Июнь/Июль/Август 2026) |
| `input\Din_TP.xlsx` | `Лист_1` | [Клиент] + 3 столбца месяцев (динамически) |

Файлы динамики могут менять месяцы — всегда бери столбцы со 2-го по 4-й по заголовкам файла, захардкодить названия месяцев нельзя.

## ЧАСТЬ 1: СИНХРОНИЗАЦИЯ (жёсткая изоляция)
**Изоляция:** [Тип лида]=='Рарос' → поиск ТОЛЬКО в `Din_RRS.xlsx`; =='ТатПартс' → ТОЛЬКО в `Din_TP.xlsx`. Перекрёстный поиск ЗАПРЕЩЁН.
**Пустой [Тип лида]:** [Клиент в динамике]='Не найдено', [Рекомендуемая стадия]='Ошибка типа лида'; правила квалификации к таким строкам НЕ применять; сортируются в конце.

### Нормализация normalize(text)
1. UPPER; замена похожих латинских на кириллицу: C→С, O→О, A→А, E→Е, P→Р, H→Н, K→К, M→М, T→Т, X→Х, B→В, Y→У.
2. Удалить: содержимое скобок `(…)`, `[…]`, кавычки; телефоны (`+7…`, `89…`, 10+ цифр подряд); адреса (`ул.`, `проезд`, `шоссе`, `литер`, `г.Город`, `д.`, `корпус`, `офис`, `пос.`, `СНТ`, `км`); префикс `ТОП -`.
3. Удалить тех. метки: `\b\d+\s*ДН\b`, `ЭДО`, `СКАН`, `QR`, `БЕЗ ДОГОВОРА`, `Б/Д`, `РАСПРЕДЕЛЯТЬ ОПЛАТЫ`, `+++`.
4. Удалить орг. формы (как отдельные слова): ООО, ИП, АО, ПАО, ОАО, МУП, УП, НПО, СЗ, ССК.
5. Удалить стоп-слова городов: САМАРА, МОСКВА, ЖИГУЛЕВСК, КАЗАНЬ, ЗЕЛЕНОДОЛЬСК, АЗИНО, КВАРТАЛ, КВАРТАЛА, ВОЛЖСК, АЛЕКСЕЕВСКОЕ.
6. Удалить все цифры, пунктуацию; схлопнуть пробелы.

### Приоритеты матчинга (первый успешный уровень побеждает)
1. **Exact** — совпадение нормализованных строк как МНОЖЕСТВ токенов (порядок не важен).
2. **Core** — совпадение значимых слов (>3 симв., исключая ТРАНС, СЕРВИС, СТРОЙ, САМАРА, АВТО и стопслова городов): ядро одной стороны ⊆ ядро другой, пересечение непусто. Только если сделка НЕ похожа на ФИО (не ИП).
3. **Abbr** — аббревиатуры (2–5 заглавных букв, искл. ООО, ИП, ТОП и стопслова) из исходной строки KB встречаются в исходной строке клиента 1С (например «РШК», «ПЛК»).
4. **IP** — для ФИО/ИП: совпадение фамилии + первых букв имени-отчества на общем префиксе (2-я буква, если есть у обеих сторон, тоже совпадает).

Никакого fuzzy-matching — только детерминированные правила.

## ЧАСТЬ 2: КВАЛИФИКАЦИЯ (для каждой строки KB индивидуально)
- **Шаг А:** Потенциал → число из текста ("600 000,00 руб."→600000); пусто/без цифр → 0. Месяцы динамики → float, NaN → 0.
- **Шаг Б:** найдено → Avg_Quarter = среднее трёх месяцев; не найдено → 0.
- **Шаг В:** Project_Potential: если для одного [Название сделки] в KB есть и 'Рарос', и 'ТатПартс' → Потенциал/2, иначе полный Потенциал.
- **Шаг Г (строго по порядку):**
  - БЛОК 1 (не найдено): Потенциал < 1000 → «Проверьте первый заказ и заполните потенциал»; иначе → «Проверьте первый заказ».
  - БЛОК 2 (найдено): Потенциал < 1000 → «Ошибка, заполните потенциал»; Avg==0 → «Отвалился»; Avg ≥ Project_Potential → «Есть выборка потенциала»; иначе → «Нет выборки потенциала».

## ТРЕБОВАНИЯ К ВЫВОДУ — output\Output.xlsx (перезаписывать; файл и папку скрипт создаёт сам)
Лист **«Результат»**: | Название сделки | Тип лида | Клиент в динамике | Стадия сделки | Потенциал закупок по Рарос и ТатПартс | Средняя выручка за квартал | Рекомендуемая стадия | Бизнес-регион |
- [Бизнес-регион] переносится из KB без изменений последней колонкой; если столбца в KB нет — колонка выводится пустой.
- Сортировка: сначала 'Рарос', затем 'ТатПартс', затем пустые типы; внутри — по названию.
- Числовые колонки — числами с форматом разрядов тысяч (`# ##0,00`); для ненайденных: «Не найдено», Avg=0.
Лист **«Сводка»**: Всего строк; Синхронизировано успешно A (N%); Не найдено B (N%); распределение по всем рекомендуемым стадиям (включая «Ошибка типа лида»).
В консоль — полная таблица и сводка (настройки выводимости pandas).

## Проверка после запуска
- Открыть `output\Output.xlsx`, убедиться: строк результата = строк KB; «Ошибка типа лида» ровно у строк с пустым типом; выборочно сверить матчи (КЛЕВЕР ТРАК, РЕМЗОНА-ДЕТАЛЬ, РШК↔ТатПартс, ПЛК↔ТатПартс, Милктранс).
